dotfiles
========

Config files from ~/

## Installation

cd dotfiles
./install

This script will backup your old dotfiles to ~/dotfiles.bak and ln -s the new.

If you're coming from screen and miss:
  screen -DRR
use:
  tmux -2 a -d || tmux -2 new
(-2 forces 256 colors)

" vim:ft=mkd
